<%= form_for(@debt) do |f| %>
  <% if @debt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@debt.errors.count, "error") %> prohibited this debt from being saved:</h2>

      <ul>
      <% @debt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :เรื่อง %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :เอกสาร%><br>
    <%= f.text_field :doc %>
  </div>
  <div class="field">
    <%= f.label :วันที่%><br>
    <%= f.date_select :day%> 
  </div>
  <div class="field">
    <%= f.label :หมายเหตุ %><br>
    <%= f.text_area :remarks %>
  </div>
  <div class="field">
  <%= f.label :squadron_id, "ฝูงบิน" %><br />
  <%= f.collection_select :squadron_id, Squadron.all, :id, :sq, :prompt => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
