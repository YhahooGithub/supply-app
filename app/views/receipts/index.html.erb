<h1 align=center>รายการรับพัสดุ</h1>
 
<table width=100%>
  <tr>
    <th>ลำดับ</th>
    <th>ทอ.74</th>
    <th>วันเดือนปี</th>
    <th>เรื่อง</th>
    <th>ใบจ่ายพัสดุ</th>
    

   
 </tr>
 
  <% @receipts.each_with_index do |receipt, index| %>
  <tr>
    <td><%= index+1 %></td>
    <td><%= link_to receipt.receipt_doc, receipt %></td>
    <td><%= receipt.receipt_date%></td>
    <td><%= receipt.receipt_title%></td>
    <td> </td>
  </tr>
  <% end %> 
</table>
<br>
