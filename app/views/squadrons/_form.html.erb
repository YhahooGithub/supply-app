<%= form_for(@squadron) do |f| %>
  <% if @squadron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@squadron.errors.count, "error") %> prohibited this squadron from being saved:</h2>

      <ul>
      <% @squadron.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :wing_id, "Wing" %><br>
    <%= f.collection_select :wing_id, Wing.all, :id, :name, prompt: true %>
  </div>
  <div class="field">
    <%= f.label :sq, "Squadron" %><br>
    <%= f.text_field :sq %>
  </div>
  <div class="field">
    <%= f.label :contact_name, "Contact Name" %><br>
    <%= f.text_field :contact_name %>
  </div>
  <div class="field">
    <%= f.label :contact_number, "Contact Number" %><br>
    <%= f.text_field :contact_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
